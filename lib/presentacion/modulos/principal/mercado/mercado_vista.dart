import 'package:flutter/material.dart';

class MercadoVista extends StatelessWidget {
  const MercadoVista({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Mercado vista'),
    );
  }
}