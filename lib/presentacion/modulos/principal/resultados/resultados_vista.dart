import 'package:flutter/material.dart';

class ResultadosVista extends StatelessWidget {
  const ResultadosVista({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Inicio vista'),
    );
  }
}