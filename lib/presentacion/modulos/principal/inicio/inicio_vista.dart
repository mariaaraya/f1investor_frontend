import 'package:flutter/material.dart';

class InicioVista extends StatelessWidget {
  const InicioVista({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Inicio vista'),
    );
  }
}