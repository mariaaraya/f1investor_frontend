import 'package:flutter/material.dart';

class PerfilVista extends StatelessWidget {
  const PerfilVista({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Perfil vista'),
    );
  }
}